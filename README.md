# Ada-SPARK-Palindrome-Pairs-Lite

Count exact reverse-word pairs, a bounded lite form of palindrome-pairs. Implemented as a small bounded Ada SPARK example with `SPARK_Mode => On`.

```sh
make test
make prove
```
